# UI Patterns (界面组件模式)

本目录定义了本项目特定的界面模式和组件范式。

## 核心内容
- **Layouts (布局模式)**：典型的手机端页面框架（如：列表详情页、沉浸式卡片流）。
- **Components (组件规格)**：标准按钮、输入框、信息卡片、底部固定操作栏等组件的结构定义。
- **Interaction (交互模式)**：滑动手势、弹窗逻辑、加载状态等全局一致的交互行为。

## 目标
实现“模块化设计”，允许 AI 通过引用已定义的模式快速组装新页面。
